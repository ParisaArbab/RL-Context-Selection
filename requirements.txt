# Core experiments (environment, policy, REINFORCE, PPO-lite, tests, plots)
# run on these alone -- no deep-learning framework required.
numpy>=1.24
matplotlib>=3.7

# Needed for the PyTorch implementation (src/context_selection/policy_torch.py,
# train_torch.py, ppo_torch.py, experiments/run_benchmark_torch.py) and its
# gradient cross-check test (tests/test_policy_torch.py). Optional: the NumPy
# implementation and its tests run fine without this installed -- the torch
# test skips cleanly if torch is missing.
torch>=2.0

# Optional: tests also run fine under pytest if you have it installed
# (they're plain unittest.TestCase, so `python -m unittest discover -s tests`
# works with zero extra dependencies).
# pytest>=7.0
